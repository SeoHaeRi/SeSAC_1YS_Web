<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.1.min.js"
        integrity="sha256-o88AwQnZB+VDvE9tvIXrMQaPlFFSUTR+nldQm1LuPXQ=" crossorigin="anonymous"></script>
    <title>cookie</title>
    <style>
        .d-none {
            display: none;
        }

        .box {
            width: 300px;
            border: solid 2px royalblue;
        }

        .modal-title {
            display: flex;
            justify-content: space-between;
            border-bottom: solid 2px royalblue;
            padding: 5px;
        }

        .modal-content {
            padding: 5px;
            text-align: center;
        }

        .modal-check {
            font-size: 10px;
            display: flex;
            align-items: center;
        }
    </style>
    <script>
        function modalClose() {
            let form = document.getElementById("form-modal");
            let check = document.getElementById("check");
            console.log(form.checkbox);
            axios({
                method: "post",
                url: "/setpopup",
                data: {
                    value: form.checkbox.checked
                }
            })
                .then((result) => {
                    if (result.data) {
                        console.log("성공");
                    }
                    $(".box").addClass("d-none");
                    // $(".main").removeClass("d-none");

                })
        }


    </script>
</head>

<body>
    <div class="box">
        <form id="form-modal">
            <div class="modal-title"><span>안내</span><button type="button" onclick="modalClose()">X</button></div>
            <div class="modal-content">2022년 12월 종료됩니다.<br> 새로운 앱을 사용해주세요.</div>
            <div class="modal-check">
                <!-- <button type="button" onclick="modalClose()">오늘 다시보지 않기</button> -->
                <input type="checkbox" name="checkbox" id="check"><span>오늘 다시보지 않기</span>
            </div>
        </form>
    </div>
    <h1 class="main">환영합니다</h1>
    <script>
        console.log("<%=popup%>");
        if ("<%=popup%>" == "none") {
            $(".box").addClass("d-none");
        }
    </script>
</body>

</html>